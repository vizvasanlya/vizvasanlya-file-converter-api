# File Converter API

A lightweight API concept for queueing conversion jobs, validating file types, and tracking processing status.

## Features

- POST /jobs
- GET /jobs
- GET /health
- Queue status tracking
- File type validation

## Install and run

```bash
npm install
npm start
```

## Health check

```bash
curl http://localhost:3000/health
```
